{
  "private": true,
  "name": "react-hook-template",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^4.2.5",
    "nodemon": "^2.0.4",
    "prettier": "^2.1.0"
  },
  "prettier": {
    "printWidth": 80,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "echo commiting"
    }
  },
  "scripts": {
    "dev": "nodemon",
    "build": "yarn build:hook && yarn build:example",
    "build:hook": "yarn workspace react-ipfs-hook run build",
    "build:example": "yarn workspace example run build",
    "publish": "yarn workspace react-ipfs-hook publish"
  },
  "version": "0.0.0"
}
